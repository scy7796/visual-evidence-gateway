# Optional local-development overrides. Keep populated files out of Git.
VISUAL_EVIDENCE_GATEWAY_CONFIG=
# Optional when Codex authentication/configuration lives outside its default location.
CODEX_HOME=
